@page
@model YogaManagement_WebApp.Pages.YogaClasses.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.YogaClass[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YogaClass[0].ClassStart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YogaClass[0].ClassEnd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YogaClass[0].AvailableFormats)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YogaClass[0].Specialities)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.YogaClass) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClassStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ClassEnd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AvailableFormats)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Specialities)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
